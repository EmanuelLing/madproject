<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Madproject" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/gold</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.MadProject.Splash" parent="Theme.Material3.Dark">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@color/gold</item> <!-- Set background color for Splash -->
    </style>

</resources>